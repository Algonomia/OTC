
{{- if and .Values.otcLlm.enabled .Values.otcLlm.persistentVolumeClaim.logs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otc-llm-logs-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    meta.helm.sh/release-name: {{ .Release.Name }}
    meta.helm.sh/release-namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.otcLlm.persistentVolumeClaim.storageClassName }}
  storageClassName: {{ .Values.otcLlm.persistentVolumeClaim.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.otcLlm.persistentVolumeClaim.logsStorage }}
{{- end }}