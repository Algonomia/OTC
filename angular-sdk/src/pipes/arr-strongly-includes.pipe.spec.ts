import { ArrStronglyIncludesPipe } from './arr-strongly-includes.pipe';

describe('ArrStronglyIncludesPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrStronglyIncludesPipe();
    expect(pipe).toBeTruthy();
  });
});
