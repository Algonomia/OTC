import { DisplayComplexValuePipe } from './display-complex-value.pipe';

describe('DisplayComplexValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayComplexValuePipe();
    expect(pipe).toBeTruthy();
  });
});
