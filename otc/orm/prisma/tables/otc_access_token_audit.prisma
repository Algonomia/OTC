model OTCAccessTokenAudit {
    accessKey       String      @id @unique @map("access_key")
    provider        String      @map("provider")
    lastUsedAt      DateTime    @map("last_used_at")

    @@map("otc_access_token_audit")
}
