/** 
 * It's a convenience pattern, if we add more decorators, we just add them to the index
 * One import for all auth-decorator
 * Usage: import { AuthUser, CurrentUser } from '../auth/decorators'
 **/ 

export * from './current-user.decorator';
